import { useTranslations } from "next-intl";

export default async function Home({ params }: { params: { locale: string } }) {
  // const t = useTranslations("Dashboard");

  return <p>FUCKING DICK</p>;
}
